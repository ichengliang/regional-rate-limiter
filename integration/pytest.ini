[pytest]
testpaths = tests
# Slow tests (e.g. ~30s TTL-bound update propagation) are deselected by default.
# Run them with `-m slow`, or everything with `-m ""`.
addopts = -v --tb=short -m "not slow"
markers =
    slow: tests that take tens of seconds (TTL-bound propagation, etc.)
