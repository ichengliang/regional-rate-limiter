[pytest]
testpaths = tests
# `slow` tests (HPA scale-up under load) are deselected by default.
addopts = -v --tb=short -m "not slow"
markers =
    slow: tests that take minutes (drive load to trigger enforcer HPA scale-up)
    ha: checks that require kubectl access to the cluster
